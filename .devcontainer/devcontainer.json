{
  "name": "scipro-container",
  "dockerFile": "Dockerfile",  // Hier wird auf das Dockerfile verwiesen
  "context": ".",  // Verwende das aktuelle Verzeichnis
  "settings": {
    "terminal.integrated.shell.linux": "/bin/bash"
  },
  "postCreateCommand": "pip install --no-cache-dir -r /tmp/requirements.txt",  // Installiere Pakete, wenn der Container erstellt wird
  "extensions": [
    "ms-python.python",
    "jupyter.jupyter-notebooks"
  ],
  "portsAttributes": {
    "8888": {
      "label": "JupyterLab",
      "onAutoForward": "openInBrowser"
    }
  }
}
