{
  "name": "My Python Devcontainer",
  "features": {
    "ghcr.io/devcontainers/features/python:1": {
      "version": "3.10"
    }
  },
  "postCreateCommand": ". /usr/local/pyenv/versions/3.8.18/bin/activate && pip install --upgrade pip && pip install -r requirements.txt && python -m ipykernel install --user --name=.venv --display-name 'Python (.venv)'",
  "customizations": {
    "vscode": {
      "settings": {
        "python.defaultInterpreterPath": ".venv/bin/python"
      },
      "extensions": [
        "ms-python.python",
        "ms-toolsai.jupyter"
      ]
    }
  }
}
